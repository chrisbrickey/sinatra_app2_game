<!-- this is the template for showing a room as the user travels through the game -->

<h1><%= room.name %> </h1>

<pre>
  <%= room.description %>
</pre>

<% if room.name == "death" || room.name == "The End" %>
  <p><a href="/">Play Again?</a></p>
<% else %>
  <p>
    <!-- below line (inside the form tag) tells the computer where to deliver this form...to the post '/game' handler in mygame.rb -->
  <form action="/game" method="POST">
    <!-- below is just text -->
    -
    <!-- input tag tells the computer which type of input (text) and the parameters to use...When something is typed into the action field, it becomes the value for params[:action] -->
    <input type="text" name="action">
    <input type="SUBMIT">
  </form>
  </p>
<% end %>
